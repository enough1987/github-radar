import React from 'react';

const Header = () => (
    <React.Fragment>
        <div className="header">
            <h1>GitHub Radar POC</h1>
        </div>
    </React.Fragment>
);

export default Header;
